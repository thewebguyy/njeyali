# ============================================================================
# Njeyali Travel - Environment Configuration
# ============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/njeyali-travel

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Email Configuration (SMTP)
# For production, use your email provider's SMTP settings
# Gmail example: smtp.gmail.com, port 465
# For Gmail, you need to create an "App Password" in your Google Account settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Njeyali Travel" <noreply@njeyalitravel.com>

# Admin Configuration
ADMIN_EMAIL=admin@njeyalitravel.com

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Security (Optional - for future use)
JWT_SECRET=your-super-secret-jwt-key-change-this
SESSION_SECRET=your-session-secret-change-this

# API Keys (Optional - for future integrations)
# GOOGLE_MAPS_API_KEY=your-google-maps-key
# PAYMENT_API_KEY=your-payment-gateway-key

# ============================================================================
# IMPORTANT NOTES:
# ============================================================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control (already in .gitignore)
# 3. For Gmail SMTP:
#    - Enable 2-factor authentication
#    - Generate an App Password (not your regular password)
#    - Use the App Password in SMTP_PASS
# 4. Change all "your-" placeholders with actual values
# 5. For production, use environment-specific values
